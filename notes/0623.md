# previosly

- binning didn't help enough

# new isdea:

- claude suggestion that makes sense

- make a giant histogram , since we don't really actually simulate agents. we only use heterodoxy, age and religion to determine when religions split

- so instead can have a 3d grid w/ those axes. each cell contains a countoa
- ie: count[6][4][Orthodox] = 118000 reads as: "118,000 Orthodox people have heterodoxy ≈ bin 6 and age ≈ band 4." Not 118,000 structs — one u64
  holding the value 118000.

  - thing is i can't represent religion as an ind. necessarily. or I could perhaps by mapping ind > religioin in a global map. other option would be to just have the base DS be hashmap of ReligionKey > histogram.

something like TerrestrialSpiritbinding: {heterodoxy 0.1: {{ageband1, ageband2}}} except not as an internal map, and the age band / deepest layer would be a count

- one thing i wanna amek sure of here is to not just have some loose Vec<Vec<number>> kinda thing, it'll be very hard to track. perhap wil use some newtypes here

{can pr0obably add what that final type ends up loking like}

pub struct HeterodoxyBin(usize);
pub struct AgeBin(usize);

- added impls for all for ergonomics, cell type so i can query cells easier
- ::new() instantiates histogram of dim heterodoxy bin num and dim age bin num, which i can set in config

# individual stage vs cohort stage etc

- think to keep some level of detail in the beginning we ought to stretch and contract the dimensionality of histogram, ie at first we can pick some higher number like 100 or 500 bins for each. that gives some more detail when it matters, once we hit some pop level we contract that into 100 or even less bins. but only if i see the performance drop

- then later might add a impl for histogram to contract it (probably won't ever expand)
- created a .bin() on histogram, (as a verb) to easily pass in a given het / age value and assign it to a bin

# simplify

- i need to kinda simplify the core loop, there's alot going on. should be easier now that it's all just numbers

core loop per tick:

1. births, deaths, aging
2. religions schism or expand > adherents convert to new religions or between established ones

- i always meant to clean thi area up too bc we were doing alot of "get rid of dead ppl" in multiple places. need to decie on one place: framing is more "who died or got born in the last 20 years until today" so we should first get rid of ppl who died (do not contribute) then calculate births from the living (during that time) and adbance eeryones age. makes much more sense this way. so then we decrement all dead, and everyone remaining from then on for other tick steps is alive (no need to check if alive etc plus don't even have this concept anymroe since everyone is just a count)

        // calc. how many die per age bin
        // well isn't this just going to be the actual death rate per band anyway? i just see what
        // band an age bin falls into (unless i just mirror the bands) and then remove death_rate *
        // count amount of ppl from it

- so what im thinking about is that now i really don't need to "flip a coin". is htis binomial or whatever? i mean at scale we end up at birth / death rate amount of ppl

- ok so this is binomial:
  one person / one coin flip > bernoulli trial
  N people flipping same probability coin > binomial

- so i don't wanna just do rate \* count cause that's just average, want to do a single binomial per bin to get better dynamics esp. at smaller numbers

## fixing struct

just realized, access would amke much more snse as ageband > het band. ie for each age band i have some spectrum of heterodoxies. bc for birth for instance, i would be adding various heterodoxies to band age(0). it would be annoyuign to basically for loop thru each heterodoxy and add to age 0. same for aging, since then i just move the whole het struct over to the next age bin, that's it.

## id

- now age bands are basically Band inclusive, so 0 band is ayone from 0 - 10 etc

## thnking

- why is this array acessing better than the map stuff from before? cause we flatten how many calcs we actually do
- even tho i end up w/ a lot of O(n^2) in worst case, n is a pretty small nimber comapred to before. we only ever have coupel hundred bands max each, he real numbers are from within, in the counts. and that flattens alot of comp. time

## death calc

- i was going to do something like
  loop thru all religions
  loop thru all age bands in histogram
  per age band, get total count, binomial flip to determine how many of those died, remove equally from each het. bin in that age band. but that actually would end up cutting disproportionately into the smallest bins, so i think instead use the Count at each cell and calculate binomial from that

## birth calc

- going to simplify the whole heterodoxy beta distr. thing. i can add complexity once i get this thing running fast
- instead will loop thru each het bin in an age band, calculate how many births for that het bin and add em as the same bin but age 0. for now that should be ok

## aging

- easy: just take the old counts, pop the last one (they're dead), create new one w/ empty first age band (will be replaced at birh step) and then push the old ones, that increments everyone up an index

### results of just age / death / birth cycle change

- very very fast. 500 gens in 0.14 seconds, but ended up w/ 10 trillion ppl after 500 gens. seems wrong i think, maybe birth too fast / death not aggresive enough

- time to do a supply demand log type curve, where we scale mortality rate up by pop / K so that we don't grow nearly as fast
- will be env depednent, ie k in a desert should be differne than grassland

- now we have it so that pops settle at equilibrium around k \* 3.4 ( basically ratio of birth / death rate rn)
- after 50k gens, still at around 16 million. now the interesting part will be the ppl within living pop moving around / forming new religions

(btw, 50k gens takes ~9 seconds!!!!)

- at this speed, we can realy start to get some cool evolutionary type physics going

## adding back schisms

- how will schisms work now ( the whole point)

1. trigger for a religon schisming was and still should be how many ppl aboce x heterodoxy threshhold / total ppl in religion, passing a % threshold.
2. conversion to the new religion would be a binomial: leaving[bin] = Binomial(count_in_bin, bin_heterodoxy \* conversion_base_rate)

## small religon change

- think it'll be much better to just have religion itself be an enum, where its either active or extinct. acive has adherent vec, extinct doesnt and instead has extinction date. that way we gotta pattern match in places where it would matter, and i can easily filter off. also bc when religon goes extinct we fully drop the prop for adeherents since its no longer neded
